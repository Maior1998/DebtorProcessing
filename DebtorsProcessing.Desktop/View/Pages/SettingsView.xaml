<Page x:Class="DebtorsProcessing.Desktop.View.Pages.SettingsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=SettingsViewModel}"
      Title="SettingsView">

    <DockPanel>
        <DockPanel LastChildFill="False" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20">
            <Button Content="Изменить пароль" Command="{Binding ChangePassword}" />
        </DockPanel>
    </DockPanel>
</Page>